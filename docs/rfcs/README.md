# RFCs

RFCs are proposals for changes that affect public contracts, product constitution, Self Model, permissions, external actions, core dependencies or cross-module architecture.

Process:

```text
Draft → Discussion → Experiment → Accepted | Rejected | Withdrawn
```

An accepted RFC is followed by an ADR when the decision is implemented. RFC history remains unchanged.

